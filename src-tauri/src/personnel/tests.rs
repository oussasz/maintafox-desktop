//! Personnel smoke tests placeholder.
//!
//! This module exists because `personnel::mod` wires `mod tests;` under
//! `#[cfg(test)]`. Keep at least one test so the module compiles consistently
//! across all test targets.

#[test]
fn personnel_tests_module_compiles() {
    assert!(true);
}
