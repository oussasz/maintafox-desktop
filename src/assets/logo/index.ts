/**
 * Maintafox logo assets.
 *
 * Usage:
 *   - Color version for light backgrounds  → logoColor / logoColorPng
 *   - White version for dark backgrounds   → logoWhite / logoWhitePng
 *
 * SVG is preferred; PNG fallbacks for contexts that don't support SVG.
 */

import logoColorPng from "./maintafox-logo-color.png";
import logoColor from "./maintafox-logo-color.svg";
import logoWhitePng from "./maintafox-logo-white.png";
import logoWhite from "./maintafox-logo-white.svg";

export { logoColor, logoColorPng, logoWhite, logoWhitePng };
