# Template Versioning And Checkpoint Model

**PRD:** §6.25

**Execution:** See [Roadmap execution](../../../README.md#roadmap-execution) in `README.md` (no long prompt blocks; task list + direct implementation in `maintafox-desktop/`).

## Tasks

- **Schema** — `inspection_templates`, versions, checkpoints.
- **Versioning** — Publish new version without breaking history.
- **Commands** — `ins.*` template CRUD.
- **UI** — Template builder.
- **Workforce bindings** — Allow template-level required role/qualification tags so scheduling can pre-validate eligible inspectors.
- **Cost/time standards** — Add checkpoint-level standard minutes to support inspection labor planning and variance tracking.

---

*Completion: date, verifier, `cargo check` / `pnpm typecheck` notes.*
