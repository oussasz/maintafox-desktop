# Git snapshots

Operational baselines captured **before** reconstruction or recovery (see `docs/BRANCHING_STRATEGY.md` §7).

Filename pattern: `YYYYMMDD-HHMM-pre-<operation>.md`

Do not store secrets. Keep recent high-risk baselines; prune obsolete snapshots in a later chore.
